import { createTheme } from "@mui/material";

const theme = createTheme({
	palette: {
		primary: {
			main: "#556cd6",
		},
		secondary: {
			main: "#19857b",
		},
		error: {
			main: "#ff3333",
		},
		background: {
			default: "#f5f5f5",
		},
	},
});

export default theme;
